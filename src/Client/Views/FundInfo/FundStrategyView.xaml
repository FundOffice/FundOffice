<UserControl
    x:Class="FMO.FundStrategyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:FMO"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shared="clr-namespace:FMO.Shared;assembly=SharedUI"
    d:DataContext="{d:DesignInstance Type=local:FundStrategyViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style
            x:Key="EditableTextDefault"
            BasedOn="{StaticResource TextBoxBaseStyle}"
            TargetType="TextBox">
            <Setter Property="MinWidth" Value="80" />
        </Style>

        <DataTemplate x:Key="DT.Default">
            <TextBox
                MinWidth="100"
                BorderThickness="0"
                Text="{Binding NewValue, UpdateSourceTrigger=PropertyChanged, Delay=100}" />
        </DataTemplate>

        <DataTemplate x:Key="DT.Date">
            <DatePicker
                MinWidth="110"
                BorderThickness="0"
                SelectedDate="{Binding Modifier.NewValue, Converter={StaticResource DataTimeDateOnlySwitchConverter}, RelativeSource={RelativeSource AncestorType=shared:ModifiableControl}}" />
        </DataTemplate>


        <DataTemplate x:Key="DT.Date.Display">
            <shared:CopyableTextBlock MinWidth="100" Text="{Binding ., StringFormat=yyyy-MM-dd}" />
        </DataTemplate>

        <DataTemplate x:Key="DT.Date.End">
            <shared:CopyableTextBlock x:Name="tb" Text="{Binding ., StringFormat=yyyy-MM-dd}" />
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding DayNumber}" Value="3652058">
                    <Setter TargetName="tb" Property="Text" Value="至今" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <Style BasedOn="{StaticResource {x:Type shared:ModifiableControl}}" TargetType="shared:ModifiableControl">
            <Setter Property="IsReadOnly" Value="{Binding IsReadOnly}" />
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock
                            Width="50"
                            Foreground="Gray"
                            Text="{Binding}" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <shared:CopyableTextBlock Text="{Binding}" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>
    <StackPanel Margin="0,10">

        <!--  策略  -->
        <DockPanel>
            <Border Background="Green" CornerRadius="4">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock
                        Margin="5,10"
                        FontSize="16"
                        Foreground="White"
                        Text="策略" />

                    <Button
                        hc:IconElement.Geometry="{StaticResource f.circle-plus}"
                        Command="{Binding AddStrategyCommand}"
                        Foreground="White"
                        Style="{StaticResource ButtonIcon}" />
                </StackPanel>
            </Border>

            <ListBox
                d:ItemsSource="{d:SampleData}"
                BorderThickness="0"
                ItemsSource="{Binding Strategies}"
                Style="{StaticResource ListBoxCustom}">
                <ListBox.ItemTemplate>
                    <DataTemplate>

                        <Grid Margin="8" Background="Transparent">

                            <DockPanel
                                x:Name="buttons"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Visibility="Collapsed">
                                <Button
                                    hc:IconElement.Geometry="{StaticResource f.floppy-disk}"
                                    Command="{Binding SaveCommand}"
                                    Style="{StaticResource ButtonIcon.Small}"
                                    Visibility="{Binding IsReadOnly, Mode=OneWay, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                                <ToggleButton
                                    hc:IconElement.Geometry="{StaticResource f.pen}"
                                    BorderThickness="0"
                                    IsChecked="{Binding IsReadOnly}"
                                    Style="{StaticResource ToggleButtonFlip.Small}">
                                    <hc:StatusSwitchElement.CheckedElement>
                                        <Viewbox
                                            Width="20"
                                            Height="20"
                                            VerticalAlignment="Center">
                                            <Path Data="{StaticResource f.pen}" Fill="Green" />
                                        </Viewbox>
                                    </hc:StatusSwitchElement.CheckedElement>
                                    <Viewbox
                                        Width="20"
                                        Height="20"
                                        VerticalAlignment="Center">
                                        <Path Data="{StaticResource f.eye}" Fill="Black" />
                                    </Viewbox>
                                </ToggleButton>
                                <Button
                                    hc:IconElement.Geometry="{StaticResource DeleteFillCircleGeometry}"
                                    Command="{Binding DataContext.DeleteStrategyCommand, RelativeSource={RelativeSource AncestorType=local:FundStrategyView}}"
                                    CommandParameter="{Binding .}"
                                    Foreground="Red"
                                    Style="{StaticResource ButtonIcon}"
                                    Visibility="{Binding IsReadOnly, Mode=OneWay, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                            </DockPanel>

                            <StackPanel>

                                <DockPanel LastChildFill="False">
                                    <shared:ModifiableControl
                                        EditTemplate="{StaticResource DT.Default}"
                                        Header="策略"
                                        Modifier="{Binding Name}" />

                                    <shared:ModifiableControl
                                        ContentTemplate="{StaticResource DT.Date.Display}"
                                        EditTemplate="{StaticResource DT.Date}"
                                        Header="起始日期"
                                        Modifier="{Binding Start}" />

                                    <shared:ModifiableControl
                                        ContentTemplate="{StaticResource DT.Date.End}"
                                        Header="结束日期"
                                        Modifier="{Binding End}">
                                        <shared:ModifiableControl.EditTemplate>
                                            <DataTemplate>
                                                <DockPanel>
                                                    <DatePicker
                                                        Width="110"
                                                        SelectedDate="{Binding NewValue.Date}"
                                                        Visibility="{Binding NewValue.IsLongTerm, Mode=OneWay, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                                                    <CheckBox
                                                        Margin="8,0"
                                                        Content="至今"
                                                        IsChecked="{Binding NewValue.IsLongTerm}" />
                                                </DockPanel>
                                            </DataTemplate>
                                        </shared:ModifiableControl.EditTemplate>
                                    </shared:ModifiableControl>
                                </DockPanel>


                                <shared:ModifiableControl
                                    Margin="0,4"
                                    HorizontalContentAlignment="Stretch"
                                    Header="策略说明"
                                    Modifier="{Binding Description}" />

                            </StackPanel>

                        </Grid>
                        <DataTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="buttons" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>

        <!--  投资经理  -->
        <DockPanel Margin="0,10,0,0">
            <Border Background="Green" CornerRadius="4">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock
                        Width="35"
                        Margin="5,10"
                        FontSize="16"
                        Foreground="White"
                        Text="投资经理"
                        TextWrapping="Wrap" />

                    <Button
                        hc:IconElement.Geometry="{StaticResource f.circle-plus}"
                        Command="{Binding AddManagerCommand}"
                        Foreground="White"
                        Style="{StaticResource ButtonIcon}" />
                </StackPanel>
            </Border>

            <ListBox
                d:ItemsSource="{d:SampleData}"
                BorderThickness="0"
                ItemsSource="{Binding Managers}"
                Style="{StaticResource ListBoxCustom}">
                <ListBox.ItemTemplate>
                    <DataTemplate>

                        <Grid Margin="8" Background="Transparent">
                            <Grid.Resources>
                                <Style BasedOn="{StaticResource {x:Type shared:ModifiableControl}}" TargetType="shared:ModifiableControl">
                                    <Setter Property="IsReadOnly" Value="{Binding IsReadOnly}" />
                                </Style>
                            </Grid.Resources>
                            <DockPanel
                                x:Name="buttons"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Visibility="Collapsed">
                                <Button
                                    hc:IconElement.Geometry="{StaticResource f.floppy-disk}"
                                    Command="{Binding SaveCommand}"
                                    Style="{StaticResource ButtonIcon.Small}"
                                    Visibility="{Binding IsReadOnly, Mode=OneWay, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                                <ToggleButton
                                    hc:IconElement.Geometry="{StaticResource f.pen}"
                                    BorderThickness="0"
                                    IsChecked="{Binding IsReadOnly}"
                                    Style="{StaticResource ToggleButtonFlip.Small}">
                                    <hc:StatusSwitchElement.CheckedElement>
                                        <Viewbox
                                            Width="20"
                                            Height="20"
                                            VerticalAlignment="Center">
                                            <Path Data="{StaticResource f.pen}" Fill="Green" />
                                        </Viewbox>
                                    </hc:StatusSwitchElement.CheckedElement>
                                    <Viewbox
                                        Width="20"
                                        Height="20"
                                        VerticalAlignment="Center">
                                        <Path Data="{StaticResource f.eye}" Fill="Black" />
                                    </Viewbox>
                                </ToggleButton>
                                <Button
                                    hc:IconElement.Geometry="{StaticResource DeleteFillCircleGeometry}"
                                    Command="{Binding DataContext.DeleteManagerCommand, RelativeSource={RelativeSource AncestorType=local:FundStrategyView}}"
                                    CommandParameter="{Binding .}"
                                    Foreground="Red"
                                    Style="{StaticResource ButtonIcon}"
                                    Visibility="{Binding IsReadOnly, Mode=OneWay, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                            </DockPanel>

                            <StackPanel>


                                <DockPanel LastChildFill="False">
                                    <shared:ModifiableControl Header="基金经理" Modifier="{Binding Person}">
                                        <shared:ModifiableControl.EditTemplate>
                                            <DataTemplate>
                                                <ComboBox
                                                    Width="120"
                                                    ItemsSource="{Binding DataContext.Managers, RelativeSource={RelativeSource AncestorType=shared:ModifiableControl}}"
                                                    SelectedItem="{Binding NewValue}">
                                                    <ComboBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <TextBlock Text="{Binding Name}" />
                                                        </DataTemplate>
                                                    </ComboBox.ItemTemplate>
                                                </ComboBox>
                                            </DataTemplate>
                                        </shared:ModifiableControl.EditTemplate>
                                    </shared:ModifiableControl>

                                    <shared:ModifiableControl
                                        ContentTemplate="{StaticResource DT.Date.Display}"
                                        EditTemplate="{StaticResource DT.Date}"
                                        Header="起始日期"
                                        Modifier="{Binding Start}" />

                                    <shared:ModifiableControl
                                        ContentTemplate="{StaticResource DT.Date.End}"
                                        Header="终止日期"
                                        Modifier="{Binding End}">
                                        <shared:ModifiableControl.EditTemplate>
                                            <DataTemplate>
                                                <DockPanel>
                                                    <DatePicker
                                                        Width="110"
                                                        SelectedDate="{Binding NewValue.Date}"
                                                        Visibility="{Binding NewValue.IsLongTerm, Mode=OneWay, Converter={StaticResource Boolean2VisibilityReConverter}}" />
                                                    <CheckBox
                                                        Margin="8,0"
                                                        Content="至今"
                                                        IsChecked="{Binding NewValue.IsLongTerm}" />
                                                </DockPanel>
                                            </DataTemplate>
                                        </shared:ModifiableControl.EditTemplate>
                                    </shared:ModifiableControl>
                                </DockPanel>


                                <shared:ModifiableControl
                                    Margin="0,4"
                                    HorizontalContentAlignment="Stretch"
                                    Header="简历"
                                    Modifier="{Binding Profile}" />

                            </StackPanel>

                        </Grid>
                        <DataTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="buttons" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>

    </StackPanel>
</UserControl>
