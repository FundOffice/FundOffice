<UserControl
    x:Class="FMO.ElementsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:FMO"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:FMO.Models;assembly=Models"
    xmlns:shared="clr-namespace:FMO.Shared;assembly=SharedUI"
    d:DataContext="{d:DesignInstance Type=local:ElementsViewModel}"
    d:DesignHeight="950"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>

        <ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/FactorDataTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <DataTemplate x:Key="DefaultCopyable">
                <shared:CopyableTextBlock Text="{Binding}" TextWrapping="Wrap" />
            </DataTemplate>

            <DataTemplate x:Key="FactorEditTemplate">
                <ContentPresenter VerticalAlignment="Center" Content="{Binding}" />
            </DataTemplate>

            <Style BasedOn="{StaticResource {x:Type shared:FactorModifiableControl}}" TargetType="shared:FactorModifiableControl">
                <Setter Property="IsReadOnly" Value="{Binding IsReadOnly}" />
                <Setter Property="MinHeight" Value="22" />
                <Setter Property="EditTemplate" Value="{StaticResource FactorEditTemplate}" />
                <Setter Property="ContentTemplate" Value="{StaticResource DefaultCopyable}" />
            </Style>


            <Style BasedOn="{StaticResource Modifiable.Inherit}" TargetType="shared:ModifiableControl">
                <Setter Property="IsReadOnly" Value="{Binding IsReadOnly}" />
                <Setter Property="MinHeight" Value="24" />
                <Setter Property="EditTemplate" Value="{StaticResource FactorEditTemplate}" />
                <Setter Property="ContentTemplate" Value="{StaticResource DefaultCopyable}" />
            </Style>


            <Style
                x:Key="Modifiable.Bank"
                BasedOn="{StaticResource {x:Type shared:ModifiableControl}}"
                TargetType="{x:Type shared:ModifiableControl}">
                <Setter Property="IsReadOnly" Value="{Binding IsReadOnly}" />
                <Setter Property="ContentTemplate" Value="{StaticResource DT.Bank.Display}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type shared:ModifiableControl}">
                            <Border
                                x:Name="RootBorder"
                                Padding="{TemplateBinding Padding}"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <!--  Header 区域（可定制）  -->
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <!--  内容区域（只读/编辑自动切换）  -->
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <!--  🔹 Header：支持 Header + HeaderTemplate  -->
                                    <hc:SimplePanel VerticalAlignment="Center">
                                        <TextBlock
                                            Width="14"
                                            VerticalAlignment="Center"
                                            Text="{TemplateBinding Header}"
                                            TextElement.FontWeight="Medium"
                                            TextWrapping="Wrap" />

                                        <Viewbox
                                            Height="7.5"
                                            Margin="-5,0"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top"
                                            Visibility="{Binding Modifier.IsInherited, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}">
                                            <Path Data="{StaticResource f.arrow-turn-down}" Fill="Brown" />
                                        </Viewbox>
                                    </hc:SimplePanel>


                                    <!--  🔹 内容区域：根据 IsReadOnly 切换模板  -->

                                    <ContentPresenter
                                        Grid.Column="1"
                                        Margin="5,0,0,0"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Content="{Binding Modifier.NewValue, RelativeSource={RelativeSource TemplatedParent}}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}" />

                                    <ContentPresenter
                                        Grid.Column="1"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Content="{Binding Modifier.NewValue, RelativeSource={RelativeSource TemplatedParent}}"
                                        ContentTemplate="{TemplateBinding EditTemplate}"
                                        Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityReConverter}}" />

                                    <Border
                                        Grid.Column="2"
                                        Margin="5,0"
                                        VerticalAlignment="Center"
                                        BorderBrush="Gray"
                                        CornerRadius="5"
                                        Visibility="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityReConverter}}">
                                        <StackPanel Margin="2,0">

                                            <hc:SimplePanel
                                                x:Name="clpanel"
                                                DockPanel.Dock="Right"
                                                Visibility="Collapsed">
                                                <Button
                                                    x:Name="clear"
                                                    Padding="2,1"
                                                    hc:IconElement.Geometry="{StaticResource f.circle-xmark}"
                                                    hc:IconElement.Height="14"
                                                    Command="{Binding Modifier.ClearCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                                    Foreground="Red"
                                                    Style="{StaticResource ButtonIcon.Small}"
                                                    ToolTip="清空"
                                                    Visibility="{Binding Modifier.CanClear, Converter={StaticResource Boolean2VisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}" />
                                            </hc:SimplePanel>

                                            <Button
                                                Command="{Binding Modifier.ApplyCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                                DockPanel.Dock="Right"
                                                Style="{StaticResource SaveButton}"
                                                ToolTip="确认修改"
                                                Visibility="{Binding Modifier.CanConfirm, Converter={StaticResource Boolean2VisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}" />

                                            <Button
                                                Command="{Binding Modifier.ResetCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                                DockPanel.Dock="Right"
                                                Style="{StaticResource ResetButton}"
                                                ToolTip="还原"
                                                Visibility="{Binding Modifier.CanReset, Converter={StaticResource Boolean2VisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}" />

                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>

                            <!--  🔑 状态驱动触发器  -->
                            <ControlTemplate.Triggers>


                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="clpanel" Property="Visibility" Value="Visible" />
                                </Trigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>



            <Style BasedOn="{StaticResource {x:Type shared:ChangeableContainer}}" TargetType="shared:ChangeableContainer">
                <Setter Property="Margin" Value="0,1" />
                <Setter Property="Entity" Value="{Binding RelativeSource={RelativeSource AncestorType=local:ElementsView}, Path=DataContext}" />
                <Setter Property="IsReadOnly" Value="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}}" />
            </Style>

            <Style BasedOn="{StaticResource {x:Type local:PortionElementView}}" TargetType="local:PortionElementView">
                <Setter Property="IsReadOnly" Value="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}}" />
                <Setter Property="CanDivide" Value="{Binding OnlyOneShare, Converter={StaticResource Boolean2BooleanReConverter}}" />
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="BorderThickness" Value="0,0,0,1" />
                <Setter Property="Padding" Value="1" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>

            <Style BasedOn="{StaticResource ComboBox.Small}" TargetType="ComboBox">
                <Setter Property="HorizontalContentAlignment" Value="Center" />
            </Style>

            <Style BasedOn="{StaticResource DatePicker.Small}" TargetType="DatePicker">
                <Setter Property="HorizontalContentAlignment" Value="Center" />
            </Style>

            <SolidColorBrush x:Key="LabelBrush">Gray</SolidColorBrush>

            <Thickness
                x:Key="LineSpacing"
                Bottom="2"
                Left="0"
                Right="0"
                Top="2" />



            <Style x:Key="ElementContainer" TargetType="ContentControl">
                <Setter Property="Content" Value="{Binding}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ContentControl">
                            <DockPanel Background="Transparent" LastChildFill="False">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    Foreground="{StaticResource LabelBrush}"
                                    Text="{Binding Label}" />
                                <shared:CopyableControl Margin="10,0,0,0" Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityConverter}}">
                                    <TextBlock Text="{Binding Display}" />
                                </shared:CopyableControl>


                                <DockPanel Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityReConverter}}">
                                    <ContentPresenter
                                        Margin="4,0"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}" />

                                    <Button Style="{StaticResource ResetElement}" />
                                    <Button Style="{StaticResource SaveElement}" />
                                    <Grid x:Name="x" Visibility="Collapsed">
                                        <Button Style="{StaticResource DeleteElement}" />
                                    </Grid>
                                </DockPanel>
                            </DockPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="x" Property="Visibility" Value="Visible" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="HorizontalContentAlignment" Value="Stretch">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ContentControl">
                                    <DockPanel Background="Transparent">
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            Foreground="{StaticResource LabelBrush}"
                                            Text="{Binding Label}" />
                                        <shared:CopyableControl Margin="10,0,0,0" Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityConverter}}">
                                            <TextBlock Text="{Binding Display}" />
                                        </shared:CopyableControl>

                                        <DockPanel Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityReConverter}}">
                                            <Button DockPanel.Dock="Right" Style="{StaticResource SaveElement}" />

                                            <Button DockPanel.Dock="Right" Style="{StaticResource ResetElement}" />

                                            <Grid
                                                x:Name="x"
                                                DockPanel.Dock="Right"
                                                Visibility="Collapsed">
                                                <Button Style="{StaticResource DeleteElement}" />
                                            </Grid>

                                            <ContentPresenter
                                                Margin="4,0"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}" />

                                        </DockPanel>

                                    </DockPanel>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="x" Property="Visibility" Value="Visible" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>



            <ControlTemplate x:Key="Element" TargetType="ContentControl">
                <DockPanel Background="Transparent">
                    <TextBlock
                        VerticalAlignment="Center"
                        Foreground="{StaticResource LabelBrush}"
                        Text="{Binding Label}" />
                    <shared:CopyableControl Margin="10,0,0,0" Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityConverter}}">
                        <TextBlock Text="{Binding Display}" />
                    </shared:CopyableControl>
                    <DockPanel Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityReConverter}}">

                        <ContentPresenter
                            Margin="4,0"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}" />
                    </DockPanel>
                </DockPanel>
            </ControlTemplate>


            <ControlTemplate x:Key="Element.Left" TargetType="ContentControl">
                <DockPanel Background="Transparent" LastChildFill="False">
                    <TextBlock
                        VerticalAlignment="Center"
                        Foreground="{StaticResource LabelBrush}"
                        Text="{Binding Label}" />

                    <shared:CopyableControl Margin="10,0,0,0" Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityConverter}}">
                        <TextBlock Text="{Binding Data.Old}" />
                    </shared:CopyableControl>

                    <DockPanel Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityReConverter}}">
                        <ContentPresenter
                            Margin="4,0"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}" />
                        <Button DockPanel.Dock="Right" Style="{StaticResource SaveElement}" />

                        <Button DockPanel.Dock="Right" Style="{StaticResource ResetElement}" />

                        <Grid
                            x:Name="x"
                            DockPanel.Dock="Right"
                            Visibility="Collapsed">
                            <Button Style="{StaticResource DeleteElement}" />
                        </Grid>
                    </DockPanel>
                </DockPanel>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="x" Property="Visibility" Value="Visible" />
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>



            <ControlTemplate x:Key="ExItem" TargetType="ContentControl">
                <DockPanel>
                    <TextBlock
                        VerticalAlignment="Center"
                        Foreground="{StaticResource LabelBrush}"
                        Text="{Binding Label}" />

                    <shared:CopyableControl Margin="10,0,0,0" Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityConverter}}">
                        <TextBlock Text="{Binding Data.Old}" />
                    </shared:CopyableControl>

                    <DockPanel Visibility="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityReConverter}}">
                        <Button
                            hc:IconElement.Geometry="{StaticResource f.circle-check}"
                            Command="{Binding DataContext.ModifyCommand, RelativeSource={RelativeSource AncestorType=local:ElementsView}}"
                            CommandParameter="{Binding .}"
                            DockPanel.Dock="Right"
                            Visibility="{Binding CanConfirm, Converter={StaticResource Boolean2VisibilityConverter}}" />

                        <ComboBox
                            Width="80"
                            Margin="4,0"
                            ItemsSource="{Binding Source={x:Static local:ElementsViewModel.FundModes}}"
                            SelectedItem="{Binding Data.New}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ., Converter={StaticResource EnumDescriptionConverter}}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <TextBox
                            x:Name="other"
                            MinWidth="100"
                            Text="{Binding Extra.New, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                            Visibility="Collapsed" />
                    </DockPanel>
                </DockPanel>
            </ControlTemplate>




            <DataTemplate x:Key="Element.PerformanceBenchmarks">
                <DockPanel LastChildFill="False">

                    <CheckBox
                        x:Name="ck"
                        Margin="4,0"
                        VerticalAlignment="Center"
                        Content="无"
                        IsChecked="{Binding IsAdopted.New}" />


                    <TextBox
                        x:Name="mon"
                        MinWidth="140"
                        HorizontalContentAlignment="Center"
                        Text="{Binding Data.New, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                        Visibility="{Binding IsAdopted.New, Converter={StaticResource Boolean2VisibilityConverter}}" />

                </DockPanel>
                <DataTemplate.Triggers>
                    <DataTrigger Binding="{Binding IsAdopted.New}" Value="True">
                        <Setter TargetName="ck" Property="Content" Value="有" />
                    </DataTrigger>
                </DataTemplate.Triggers>
            </DataTemplate>


            <DataTemplate x:Key="Portion.Sealing">
                <DockPanel LastChildFill="False">


                    <ComboBox
                        Width="70"
                        Margin="4,0"
                        ItemsSource="{Binding DataContext.SealingTypes, RelativeSource={RelativeSource AncestorType=local:ElementsView}}"
                        SelectedItem="{Binding NewValue.Type}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ., Converter={StaticResource EnumDescriptionConverter}}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>


                    <TextBox
                        x:Name="mon"
                        MinWidth="40"
                        Text="{Binding Data.New, UpdateSourceTrigger=PropertyChanged, Delay=300, StringFormat={}{0}个月}"
                        Visibility="Collapsed" />

                    <TextBox
                        x:Name="other"
                        MinWidth="100"
                        Text="{Binding Extra.New, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                        Visibility="Collapsed" />
                </DockPanel>
                <DataTemplate.Triggers>
                    <DataTrigger Binding="{Binding NewValue.Type}" Value="Other">
                        <Setter TargetName="other" Property="Visibility" Value="Visible" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding NewValue.Type}" Value="Has">
                        <Setter TargetName="mon" Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </DataTemplate.Triggers>
            </DataTemplate>






            <Style x:Key="CategoryTab" TargetType="TextBlock">
                <Setter Property="MaxWidth" Value="20" />
                <Setter Property="Margin" Value="8" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>





            <DataTemplate x:Key="DT.Strategy">
                <TextBox
                    AcceptsReturn="True"
                    FontSize="10"
                    Text="{Binding NewValue, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                    TextWrapping="Wrap" />
            </DataTemplate>

            <DataTemplate x:Key="DT.InvestmentObjective">
                <TextBox MinWidth="400" Text="{Binding NewValue, UpdateSourceTrigger=PropertyChanged, Delay=300}" />
            </DataTemplate>


            <DataTemplate x:Key="DefaultDT">
                <ContentPresenter Content="{Binding NewValue}" />
            </DataTemplate>

            <DataTemplate DataType="{x:Type local:PerformanceBenchmarkViewModel}">
                <DockPanel>
                    <CheckBox IsChecked="{Binding Has}" />
                    <TextBox
                        MinWidth="100"
                        HorizontalAlignment="Stretch"
                        Text="{Binding Benchmark, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                        Visibility="{Binding Has, Converter={StaticResource Boolean2VisibilityConverter}}" />
                </DockPanel>
            </DataTemplate>



            <!--<DataTemplate x:Key="DT.Agent">
            <DockPanel>
                <CheckBox VerticalContentAlignment="Center" IsChecked="{Binding HasAgency}" />
                <TextBox
                    MinWidth="180"
                    HorizontalContentAlignment="Center"
                    Text="{Binding NewValue, UpdateSourceTrigger=PropertyChanged, Delay=300}" />

            -->
            <!--<hc:AutoCompleteTextBox
                    MinWidth="180"
                    HorizontalContentAlignment="Center"
                    ItemsSource="{Binding TrusteeName}"
                    Text="{Binding NewValue, UpdateSourceTrigger=PropertyChanged, Delay=300}" />-->
            <!--
            </DockPanel>
        </DataTemplate>-->

            <DataTemplate x:Key="DT.Text.Center">
                <TextBox HorizontalContentAlignment="Center" Text="{Binding NewValue, UpdateSourceTrigger=PropertyChanged, Delay=300}" />
            </DataTemplate>


            <ControlTemplate x:Key="CT.Fee" TargetType="shared:ChangeableContainer">
                <DockPanel
                    HorizontalAlignment="Left"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    Background="Transparent"
                    DataContext="{Binding ., RelativeSource={RelativeSource Mode=TemplatedParent}}"
                    LastChildFill="True">

                    <shared:ChangeableControlButtons DockPanel.Dock="Right" />

                    <Grid>
                        <CheckBox
                            VerticalContentAlignment="Center"
                            Content="{Binding Binding.Label}"
                            IsChecked="{Binding Binding.NewValue.HasFee}" />
                        <TextBlock
                            HorizontalAlignment="Right"
                            FontSize="6"
                            Foreground="Purple"
                            Text="▼"
                            Visibility="{Binding Binding.IsInherited, Converter={StaticResource Boolean2VisibilityConverter}}" />
                    </Grid>
                    <ContentPresenter
                        x:Name="PART_Content"
                        Margin="4,0"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Content="{Binding Binding, RelativeSource={RelativeSource AncestorType=shared:ChangeableContainer}}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        Visibility="{Binding NewValue.HasFee, Converter={StaticResource Boolean2VisibilityConverter}}" />
                </DockPanel>

            </ControlTemplate>



            <DataTemplate x:Key="DT.Fee2">
                <WrapPanel DataContext="{Binding OldValue}">
                    <ComboBox
                        Width="90"
                        Margin="4,0"
                        BorderThickness="0"
                        ItemsSource="{Binding DataContext.FundFeeTypes, RelativeSource={RelativeSource AncestorType=local:ElementsView}}"
                        SelectedItem="{Binding Type}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ., Converter={StaticResource EnumDescriptionConverter}}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <TextBox
                        x:Name="mon"
                        MinWidth="40"
                        HorizontalContentAlignment="Center"
                        Text="{Binding Fee, UpdateSourceTrigger=PropertyChanged, Delay=200}" />
                    <TextBlock
                        x:Name="a"
                        Margin="4,0"
                        VerticalAlignment="Center"
                        FontSize="14"
                        Text="%"
                        Visibility="Collapsed" />
                    <TextBlock
                        x:Name="b"
                        Margin="4,0"
                        VerticalAlignment="Center"
                        FontSize="14"
                        Text="元"
                        Visibility="Collapsed" />
                    <TextBox
                        x:Name="other"
                        MinWidth="300"
                        Text="{Binding Other, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                        Visibility="Collapsed" />
                    <CheckBox
                        Margin="4,0"
                        Content="保底"
                        IsChecked="{Binding HasGuaranteedFee}" />


                    <TextBox
                        x:Name="gr"
                        MinWidth="60"
                        HorizontalContentAlignment="Center"
                        Text="{Binding GuaranteedFee, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                        Visibility="{Binding HasGuaranteedFee, Converter={StaticResource Boolean2VisibilityConverter}}" />

                    <TextBlock
                        x:Name="ad"
                        Margin="4,0"
                        VerticalAlignment="Center"
                        FontSize="14"
                        Text="元"
                        Visibility="{Binding HasGuaranteedFee, Converter={StaticResource Boolean2VisibilityConverter}}" />
                </WrapPanel>
                <DataTemplate.Triggers>
                    <DataTrigger Binding="{Binding OldValue.Type}" Value="Other">
                        <Setter TargetName="other" Property="Visibility" Value="Visible" />
                        <Setter TargetName="mon" Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding OldValue.Type}" Value="Ratio">
                        <Setter TargetName="a" Property="Visibility" Value="Visible" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding OldValue.Type}" Value="Fix">
                        <Setter TargetName="b" Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </DataTemplate.Triggers>
            </DataTemplate>

            <DataTemplate x:Key="Display.Fee" />






            <DataTemplate x:Key="DT.Portion" d:DataType="local:ShareElementsViewModel">
                <DockPanel Margin="0,1">
                    <Border
                        x:Name="bd"
                        Margin="0,0,10,0"
                        Background="WhiteSmoke"
                        Visibility="{Binding DataContext.OnlyOneShare, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityReConverter}}">
                        <DockPanel Margin="8">
                            <TextBlock DockPanel.Dock="Top" Text="份额" />
                            <TextBlock HorizontalAlignment="Center" Text="{Binding Class}" />
                        </DockPanel>
                    </Border>


                    <StackPanel>

                        <DockPanel>
                            <shared:ChangeableContainer Binding="{Binding SubscriptionRule}" ContentTemplate="{StaticResource DT.Subscription}" />
                            <shared:ChangeableContainer
                                Margin="12,0,0,0"
                                Binding="{Binding SubscriptionFee}"
                                ContentTemplate="{StaticResource DT.Fee}"
                                EditTemplate="{StaticResource CT.Fee}" />
                        </DockPanel>


                        <DockPanel Margin="0,4">
                            <shared:ChangeableContainer Binding="{Binding PurchasRule}" ContentTemplate="{StaticResource DT.Purchase}" />
                            <shared:ChangeableContainer
                                Margin="12,0,0,0"
                                Binding="{Binding PurchaseFee}"
                                ContentTemplate="{StaticResource DT.Fee}"
                                EditTemplate="{StaticResource CT.Fee}" />
                        </DockPanel>


                        <!--  费用  -->
                        <WrapPanel>


                            <shared:ChangeableContainer Binding="{Binding RedemptionFee}" ContentTemplate="{StaticResource DT.Fee}" />
                            <shared:ChangeableContainer Binding="{Binding ManageFee}" ContentTemplate="{StaticResource DT.Fee}" />

                        </WrapPanel>

                        <StackPanel Margin="0,5" Orientation="Horizontal">

                            <Border Margin="10,5" />

                            <!--<ContentControl
                            ContentTemplate="{StaticResource Portion.Sealing}"
                            DataContext="{Binding LockingRule}"
                            Style="{StaticResource ElementContainer}"
                            Visibility="{Binding DataContext.IsSealingFund, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityReConverter}}" />-->



                        </StackPanel>
                    </StackPanel>

                </DockPanel>

                <DataTemplate.Triggers>
                    <DataTrigger Binding="{Binding Class}" Value="{x:Static model:FundElements.SingleShareKey}">
                        <Setter TargetName="bd" Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                </DataTemplate.Triggers>
            </DataTemplate>



            <ItemsPanelTemplate x:Key="PortionItemsPanel">
                <UniformGrid Columns="1" IsItemsHost="True" />
            </ItemsPanelTemplate>

        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="0,10">
        <ScrollViewer HorizontalScrollBarVisibility="Disabled">

            <Grid Margin="0,0,15,0" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <StackPanel.Resources>
                        <Style TargetType="Separator">
                            <Setter Property="Margin" Value="0,10" />
                        </Style>
                    </StackPanel.Resources>

                    <!--#region 基本要素-->
                    <DockPanel>
                        <Border Margin="0,0,10,0" Background="Green">
                            <TextBlock Style="{StaticResource CategoryTab}" Text="基本要素" />
                        </Border>

                        <StackPanel>
                            <shared:ModifiableControl
                                EditTemplate="{StaticResource DT.TextBox.Center}"
                                Header="基金全称"
                                Modifier="{Binding FullName}" />


                            <DockPanel>
                                <shared:ModifiableControl
                                    MinWidth="300"
                                    EditTemplate="{StaticResource DT.TextBox.Center}"
                                    Header="基金简称"
                                    Modifier="{Binding ShortName}" />

                                <shared:ModifiableControl
                                    Margin="10,0"
                                    HorizontalAlignment="Left"
                                    EditTemplate="{StaticResource DT.SecurityFundType}"
                                    Header="基金类型"
                                    Modifier="{Binding SecurityFundType}" />
                            </DockPanel>


                            <!--  风险等级 止损预警  -->
                            <DockPanel Margin="{StaticResource LineSpacing}" LastChildFill="False">

                                <shared:ModifiableControl
                                    HorizontalAlignment="Left"
                                    EditTemplate="{StaticResource DT.RiskLevel}"
                                    Header="风险等级"
                                    Modifier="{Binding RiskLevel}" />

                                <shared:ModifiableControl
                                    Margin="10,0"
                                    HorizontalAlignment="Left"
                                    EditTemplate="{StaticResource DT.DurationInMonths}"
                                    Header="存续期"
                                    Modifier="{Binding DurationInMonths}" />


                                <shared:ModifiableControl
                                    Margin="10,0"
                                    HorizontalAlignment="Left"
                                    EditTemplate="{StaticResource DT.ExpirationDate}"
                                    Header="到期日"
                                    Modifier="{Binding ExpirationDate}" />



                            </DockPanel>


                            <!--  开放日  -->
                            <hc:UniformSpacingPanel Margin="{StaticResource LineSpacing}" Spacing="10">

                                <shared:ModifiableControl
                                    HorizontalAlignment="Left"
                                    EditTemplate="{StaticResource DT.FundMode}"
                                    Header="运作方式"
                                    Modifier="{Binding FundModeInfo}" />

                                <shared:ModifiableControl
                                    HorizontalAlignment="Left"
                                    EditTemplate="{StaticResource DT.Sealing}"
                                    Header="封闭期"
                                    Modifier="{Binding SealingRule}"
                                    Visibility="{Binding DataContext.IsSealingFund, RelativeSource={RelativeSource AncestorType=local:ElementsView}, Converter={StaticResource Boolean2VisibilityReConverter}}" />


                                <shared:FactorModifiableControl
                                    HorizontalAlignment="Left"
                                    EditTemplate="{StaticResource DT.Sealing}"
                                    Header="锁定期"
                                    Modifier="{Binding LockingRule}" />



                            </hc:UniformSpacingPanel>

                            <DockPanel DockPanel.Dock="Top" LastChildFill="False">
                                <TextBlock
                                    Margin="0,0,10,0"
                                    VerticalAlignment="Center"
                                    Foreground="Gray"
                                    Text="基金份额" />

                                <Button
                                    hc:IconElement.Geometry="{StaticResource f.pencil}"
                                    Command="{Binding BeginChangedShareCommand}"
                                    CommandParameter="{Binding RelativeSource={RelativeSource Self}}"
                                    Style="{StaticResource ButtonIcon.Small}"
                                    ToolTip="修改"
                                    Visibility="{Binding IsReadOnly, Converter={StaticResource Boolean2VisibilityReConverter}}" />


                                <ListBox
                                    VerticalAlignment="Center"
                                    VerticalContentAlignment="Center"
                                    BorderThickness="0"
                                    ItemsSource="{Binding Shares}"
                                    Style="{StaticResource ListBoxCustom}">
                                    <ListBox.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel IsItemsHost="True" />
                                        </ItemsPanelTemplate>
                                    </ListBox.ItemsPanel>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <DockPanel Margin="5,0">
                                                <TextBlock Text="{Binding Name}" />
                                                <TextBlock x:Name="tb" Text=" ：" />
                                                <TextBlock
                                                    x:Name="tb2"
                                                    MinWidth="80"
                                                    Text="{Binding Requirement}" />
                                            </DockPanel>

                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding Items.Count, RelativeSource={RelativeSource AncestorType=ListBox}}" Value="1">
                                                    <Setter TargetName="tb" Property="Visibility" Value="Collapsed" />
                                                    <Setter TargetName="tb2" Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>

                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </DockPanel>
                        </StackPanel>
                    </DockPanel>
                    <!--#endregion-->

                    <Separator />


                    <!--#region 费用相关-->
                    <DockPanel>
                        <DockPanel.Resources>
                            <Style TargetType="Separator">
                                <Setter Property="Margin" Value="0,3" />
                            </Style>
                        </DockPanel.Resources>
                        <Border Margin="0,0,10,0" Background="Blue">
                            <TextBlock Style="{StaticResource CategoryTab}" Text="费用相关" />
                        </Border>

                        <StackPanel>


                            <shared:FactorModifiableControl
                                EditTemplate="{StaticResource DT.Fee}"
                                Header="管理费"
                                Modifier="{Binding ManageFee}" />

                            <shared:ModifiableControl
                                HorizontalAlignment="Left"
                                EditTemplate="{StaticResource DT.FeePay}"
                                Header="支付方式"
                                Modifier="{Binding ManageFeePay}" />

                            <Separator />

                            <shared:FactorModifiableControl
                                EditTemplate="{StaticResource DT.Subscription}"
                                Header="认购规则"
                                Modifier="{Binding SubscriptionRule}" />

                            <Separator />

                            <shared:FactorModifiableControl
                                EditTemplate="{StaticResource DT.Purchase}"
                                Header="申购规则"
                                Modifier="{Binding PurchasRule}" />

                            <Separator />


                            <shared:FactorModifiableControl
                                EditTemplate="{StaticResource DT.RedemptionFee}"
                                Header="赎回费"
                                Modifier="{Binding RedemptionFee}" />

                            <Separator />

                            <shared:FactorModifiableControl
                                EditTemplate="{StaticResource DT.LongText}"
                                Header="业绩报酬"
                                Modifier="{Binding PerformanceFeeStatement}" />


                            <Separator />


                            <!--  费用  -->
                            <shared:ModifiableControl
                                HorizontalAlignment="Left"
                                EditTemplate="{StaticResource DT.Agency}"
                                Header="托管机构"
                                Modifier="{Binding TrusteeInfo}" />



                            <shared:ModifiableControl
                                HorizontalAlignment="Left"
                                EditTemplate="{StaticResource DT.Agency}"
                                Header="外包机构"
                                Modifier="{Binding OutsourcingInfo}" />



                        </StackPanel>



                    </DockPanel>
                    <!--#endregion-->

                    <Separator />


                    <!--#region 申赎相关-->
                    <DockPanel>
                        <Border Margin="0,0,10,0" Background="Orange">
                            <TextBlock Style="{StaticResource CategoryTab}" Text="申赎相关" />
                        </Border>
                        <StackPanel>
                            <DockPanel LastChildFill="False" Visibility="{Binding IsSealingFund, Converter={StaticResource Boolean2VisibilityReConverter}}">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    Foreground="Gray"
                                    Text="固定开放日" />
                                <!--<shared:ChangeableContainer HorizontalContentAlignment="Stretch" Binding="{Binding OpenDayInfo}" />-->

                                <TextBlock
                                    Margin="20,0"
                                    VerticalAlignment="Center"
                                    Text="{Binding OpenDayInfo.NewValue}" />

                                <Button
                                    Command="{Binding SetOpenRuleCommand}"
                                    Content="开放日规则"
                                    IsEnabled="{Binding IsReadOnly, Converter={StaticResource Boolean2BooleanReConverter}}"
                                    Style="{StaticResource ButtonPrimary.Small}" />

                            </DockPanel>


                            <shared:ModifiableControl
                                HorizontalAlignment="Left"
                                Header="临时开放"
                                Modifier="{Binding TemporarilyOpenInfo}" />


                            <!--  巨额赎回  -->

                            <shared:ModifiableControl
                                HorizontalAlignment="Left"
                                Header="巨额赎回"
                                Modifier="{Binding HugeRedemption}" />



                            <!--  冷静期  -->
                            <shared:ModifiableControl
                                HorizontalAlignment="Left"
                                Header="冷静期"
                                Modifier="{Binding CoolingPeriod}" />


                            <!--  回访  -->
                            <shared:ModifiableControl
                                HorizontalAlignment="Left"
                                Header="回访"
                                Modifier="{Binding Callback}" />


                        </StackPanel>
                    </DockPanel>
                    <!--#endregion-->


                    <Separator />


                    <!--  账户  -->
                    <DockPanel Margin="{StaticResource LineSpacing}">
                        <Border Margin="0,0,10,0" Background="SaddleBrown">
                            <TextBlock Style="{StaticResource CategoryTab}" Text="账户信息" />
                        </Border>

                        <TextBlock
                            Margin="10"
                            DockPanel.Dock="Right"
                            Foreground="Gray"
                            IsHitTestVisible="False"
                            Visibility="{Binding IsReadOnly, Converter={StaticResource Boolean2VisibilityReConverter}}">
                            <Run>可复制后点击绿色按键</Run>
                            <LineBreak />
                            <Run>自动识别账户信息</Run>
                            <LineBreak />
                            <LineBreak />
                            <Run>账户 | 账户名称：xxxxxx</Run>
                            <LineBreak />
                            <Run>账号：00000</Run>
                            <LineBreak />
                            <Run>开户行：xxxx</Run>
                            <LineBreak />
                            <Run>.....</Run>
                        </TextBlock>

                        <StackPanel>


                            <shared:ModifiableControl
                                HorizontalAlignment="Left"
                                Header="募集账户"
                                Modifier="{Binding CollectionAccount}"
                                Style="{StaticResource Modifiable.Bank}" />

                            <Separator />

                            <shared:ModifiableControl
                                HorizontalAlignment="Left"
                                Header="托管账户"
                                Modifier="{Binding CustodyAccount}"
                                Style="{StaticResource Modifiable.Bank}" />



                            <!--<shared:ChangeableContainer
                                Binding="{Binding CollectionAccount}"
                                ContentTemplate="{StaticResource DT.Bank}"
                                EditTemplate="{StaticResource CT.Bank}" />


                            <shared:ChangeableContainer
                                Binding="{Binding CustodyAccount}"
                                ContentTemplate="{StaticResource DT.Bank}"
                                EditTemplate="{StaticResource CT.Bank}" />-->

                        </StackPanel>
                    </DockPanel>

                </StackPanel>


                <StackPanel Grid.Column="2">


                    <!--#region 投资相关-->
                    <DockPanel>
                        <Border Margin="0,0,10,0" Background="Orange">
                            <TextBlock Style="{StaticResource CategoryTab}" Text="投资相关" />
                        </Border>

                        <hc:UniformSpacingPanel Orientation="Vertical" VerticalSpacing="5">

                            <DockPanel>

                                <shared:ModifiableControl
                                    HorizontalAlignment="Left"
                                    EditTemplate="{StaticResource DT.StopLine}"
                                    Header="止损线"
                                    Modifier="{Binding StopLine}" />

                                <shared:ModifiableControl
                                    HorizontalAlignment="Left"
                                    EditTemplate="{StaticResource DT.StopLine}"
                                    Header="预警线"
                                    Modifier="{Binding WarningLine}" />
                            </DockPanel>


                            <shared:ModifiableControl
                                Margin="0,0,30,0"
                                HorizontalContentAlignment="Stretch"
                                Header="业绩比较基准"
                                Modifier="{Binding PerformanceBenchmark}" />

                            <shared:ModifiableControl
                                HorizontalContentAlignment="Stretch"
                                EditTemplate="{StaticResource DT.LongText}"
                                Header="投资目标"
                                Modifier="{Binding InvestmentObjective}" />


                            <shared:ModifiableControl
                                HorizontalContentAlignment="Stretch"
                                EditTemplate="{StaticResource DT.LongText}"
                                Header="投资范围"
                                Modifier="{Binding InvestmentScope}" />

                            <Separator />

                            <shared:ModifiableControl
                                HorizontalContentAlignment="Stretch"
                                EditTemplate="{StaticResource DT.LongText}"
                                Header="投资策略"
                                Modifier="{Binding InvestmentStrategy}" />


                            <Separator />

                            <shared:ModifiableControl
                                HorizontalContentAlignment="Stretch"
                                EditTemplate="{StaticResource DT.LongText}"
                                Header="投资经理"
                                Modifier="{Binding InvestmentManager}" />

                            <!--<shared:ChangeableContainer
                                HorizontalContentAlignment="Stretch"
                                Binding="{Binding InvestmentObjective}"
                                ContentTemplate="{StaticResource DT.InvestmentObjective}" />

                            <shared:ChangeableContainer
                                HorizontalContentAlignment="Stretch"
                                Binding="{Binding InvestmentScope}"
                                ContentTemplate="{StaticResource DT.LongText}" />

                            <Border Margin="20,4">
                                <Line
                                    Stroke="Gray"
                                    StrokeDashArray="4,2"
                                    StrokeThickness="1"
                                    X1="0"
                                    X2="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=ActualWidth}"
                                    Y1="0"
                                    Y2="0" />
                            </Border>

                            <shared:ChangeableContainer
                                HorizontalContentAlignment="Stretch"
                                Binding="{Binding InvestmentStrategy}"
                                ContentTemplate="{StaticResource DT.LongText}" />

                            <Border Margin="20,4">
                                <Line
                                    Stroke="Gray"
                                    StrokeDashArray="4,2"
                                    StrokeThickness="1"
                                    X1="0"
                                    X2="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=ActualWidth}"
                                    Y1="0"
                                    Y2="0" />
                            </Border>

                            <shared:ChangeableContainer
                                HorizontalContentAlignment="Stretch"
                                Binding="{Binding InvestmentManagers}"
                                ContentTemplate="{StaticResource DT.LongText}" />-->
                        </hc:UniformSpacingPanel>
                    </DockPanel>
                    <!--#endregion-->


                    <Separator />



                </StackPanel>
            </Grid>



        </ScrollViewer>
        <!--  编辑/查看  -->
        <DockPanel
            Margin="10,-25"
            HorizontalAlignment="Right"
            VerticalAlignment="Top">
            <Button
                hc:IconElement.Geometry="{StaticResource f.floppy-disk}"
                Command="{Binding SaveCommand}"
                Style="{StaticResource ButtonIcon}"
                Visibility="{Binding IsReadOnly, Converter={StaticResource Boolean2VisibilityReConverter}}" />
            <ToggleButton IsChecked="{Binding IsReadOnly}" Style="{StaticResource ToggleButtonFlip}">
                <hc:StatusSwitchElement.CheckedElement>
                    <Viewbox Width="16" Height="16">
                        <Path Data="{StaticResource f.pen}" Fill="Green" />
                    </Viewbox>
                </hc:StatusSwitchElement.CheckedElement>
                <Viewbox Width="18" Height="18">
                    <Path Data="{StaticResource f.eye}" Fill="Black" />
                </Viewbox>
            </ToggleButton>
        </DockPanel>
    </Grid>
</UserControl>
