<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:FMO.Shared">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/SharedUI;component/Themes/ChangeableControls.xaml" />
        <ResourceDictionary Source="/SharedUI;component/Themes/Editable.xaml" />
        <ResourceDictionary Source="/SharedUI;component/Themes/Copyable.xaml" />
        <ResourceDictionary Source="/SharedUI;component/Themes/NewFileControl.xaml" />
        <ResourceDictionary Source="/SharedUI;component/Themes/YearCalender.xaml" />
    </ResourceDictionary.MergedDictionaries>



    <SolidColorBrush x:Key="LabelBrush">Gray</SolidColorBrush>

    <Style
        x:Key="CalendarDayButtonBlackStyle"
        BasedOn="{StaticResource BaseStyle}"
        TargetType="{x:Type CalendarDayButton}">
        <Setter Property="MinWidth" Value="10" />
        <Setter Property="MinHeight" Value="10" />
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarDayButton}">
                    <hc:SimplePanel>
                        <Rectangle
                            Name="TodayBackground"
                            Fill="{DynamicResource DangerBrush}"
                            Opacity="0"
                            RadiusX="16"
                            RadiusY="16" />
                        <Rectangle
                            Name="SelectedBackground"
                            Fill="{DynamicResource PrimaryBrush}"
                            Opacity="0"
                            RadiusX="16"
                            RadiusY="16" />
                        <ContentPresenter
                            Name="NormalText"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            TextElement.Foreground="{DynamicResource PrimaryTextBrush}" />
                    </hc:SimplePanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <!--  ====================== 新增：Blackout 禁用日期样式（唯一修改部分） ======================  -->
        <Style.Triggers>
            <!--  日期被禁用 → 变灰 + 半透明 + 禁止点击 + 禁用鼠标  -->
            <Trigger Property="IsBlackedOut" Value="True">
                <Setter Property="TextElement.Foreground" Value="{DynamicResource ThirdlyTextBrush}" />
                <Setter Property="Opacity" Value="0.4" />
                <Setter Property="IsHitTestVisible" Value="False" />
                <Setter Property="Cursor" Value="No" />
            </Trigger>
        </Style.Triggers>
        <!--  ======================================================================================  -->
    </Style>

    <Style
        x:Key="CalendarBlackStyle"
        BasedOn="{StaticResource CalendarBaseStyle}"
        TargetType="{x:Type Calendar}">
        <Setter Property="Calendar.CalendarDayButtonStyle" Value="{StaticResource CalendarDayButtonBlackStyle}" />
    </Style>

</ResourceDictionary>
